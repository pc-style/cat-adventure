# Copy to .env and fill in values for local development.
# Never commit .env — it is gitignored.
#
# Example placeholders (rename/remove as your project takes shape):
# API_KEY=
# DATABASE_URL=
# DEBUG=false
